{
  "name": "revenue-expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Australian personal finance, investment and tax-year tracker for retail investors, property investors, retirees and pensioners.",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "postinstall": "npm run prisma:generate --workspace=backend",
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build:frontend && npm run build:backend",
    "build:backend": "npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "db:sync": "node scripts/db-sync.js",
    "build:vercel": "npm run db:sync && npm run build:frontend",
    "start": "npm run start --workspace=backend",
    "test": "npm run test --workspace=backend",
    "test:backend": "npm run test --workspace=backend",
    "prisma:generate": "npm run prisma:generate --workspace=backend",
    "prisma:migrate": "npm run prisma:migrate --workspace=backend",
    "prisma:deploy": "npm run prisma:deploy --workspace=backend",
    "prisma:push": "npm run prisma:push --workspace=backend",
    "prisma:seed": "npm run prisma:seed --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
