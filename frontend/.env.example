# The dev server proxies /api to the backend (see vite.config.ts). Only
# needed if you want to point the frontend at a backend running somewhere
# other than http://localhost:4000.
VITE_API_BASE_URL=
